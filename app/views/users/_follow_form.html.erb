    <% if current_user?(@user) %>
    <div id="follow_form">
        <% unless current_user.following?(@user) %>
         <%= render 'unfollow' %>
       <% else %>
         <%= render 'follow' %>
    <% end %>
    </div>
    <% end %>
